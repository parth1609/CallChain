[project]
name = "CallChain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "groq>=0.36.0",
    "load-dotenv>=0.1.0",
    "openai>=2.8.1",
    "pytest>=9.0.1",
]
